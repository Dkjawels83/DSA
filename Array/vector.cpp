#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> vec = {1, 2, 3, 4, 5}; // Added missing semicolon
    cout << vec[0];
    return 0; // Added return statement (optional but recommended for main)
}

